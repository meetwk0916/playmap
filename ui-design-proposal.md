# 亲子游玩地图 · 全新 UI 设计方案

> **状态（2026-08-21）**：历史设计提案。视觉令牌、地图首页和类别筛选已部分落地；双 Tab、清单、评分/记录和社区化 FAB 已被后续“出行决策最小流程”取代。当前产品合同以 `README.md` 与 `CONTEXT.md` 为准。
>
> **设计师**：像素君（UI Designer）
> **日期**：2026-08-20
> **参考范例**：Pienten Suomi（pientensuomi.fi）— 芬兰家庭社区地图
> **原配套原型**：已于 2026-08-21 清场删除；本文保留设计决策背景

---

## 一、参考分析与设计定位

### 1.1 Pienten Suomi 值得借鉴的 5 个点

| # | 参考点 | 我们的迁移策略 |
|---|--------|----------------|
| 1 | **地图即首页**：打开即是全屏地图 + 彩色图钉，零学习成本 | 保留现有地图首页，强化图钉的类别色彩编码 |
| 2 | **社区共建**：任何家庭可通过 Tip Form 推荐目的地，内容来自真实家长 | 「＋」从"个人标记"升级为"推荐好去处"，文案强调"帮助其他家庭" |
| 3 | **儿童视角**：以"孩子觉得什么好玩"组织内容，而非成人 POI 逻辑 | 类别、筛选、详情字段全部围绕亲子实用信息（推车/卫生间/停车） |
| 4 | **北欧清新视觉**：柔和粉彩、大留白、圆润形状、俏皮但克制 | 从现行"暖黄+橙"升级为"云杉绿+暖阳黄+天空蓝"的清晨自然色系 |
| 5 | **移动优先**（90%+ 移动访问）：单手可达、大触控目标 | 核心操作全部下沉至拇指热区，触控目标 ≥ 44px |

### 1.2 目标用户与使用场景

- **谁**：25–44 岁家长（女性为主），带 3–12 岁孩子出行
- **场景**：周末早晨单手抱娃单手刷手机 → 10 秒内找到"附近适合遛娃的地方"
- **情绪目标**：打开 App 像翻开一本绘本——轻松、温暖、有探索欲

### 1.3 品牌调性

**「玩具般俏皮 × 北欧清新」**：圆润有机的形状 + 清晨自然的色彩 + 绘本式插画感，但信息层级保持成人工具的效率。

---

## 二、设计令牌（Design Tokens）

### 2.1 色彩系统

```
/* 基础色 —— 中性色整体偏向品牌绿，杜绝纯黑纯白 */
--paper:      #F7F5EE   /* 页面底色：暖纸白 */
--surface:    #FFFEFA   /* 卡片面：微暖白 */
--ink:        #24352B   /* 主文字：深云杉（on paper 对比 12.8:1）*/
--ink-2:      #5C6B60   /* 次要文字（on paper 对比 5.1:1 ✅AA）*/
--line:       #E7E2D2   /* 分隔线/描边：米灰 */

/* 品牌色 */
--spruce:     #2F6B4F   /* 主色·云杉绿（白字填充面对比 5.9:1 ✅AA）*/
--spruce-deep:#23543D   /* 主色按压态 */
--spruce-soft:#E3EFE6   /* 主色浅底 */
--sun:        #F2B33D   /* 点缀·暖阳黄（仅图标/装饰，不承载白字）*/
--sun-soft:   #FCF0D8
--sky:        #3D7AAD   /* 信息·天空蓝（白字 4.6:1 ✅AA）*/
--sky-soft:   #E2EEF6
--coral:      #D96A55   /* 社区/爱心·珊瑚红（白字 4.5:1 ✅AA）*/
--coral-soft: #FAE8E3

/* 语义色 */
--visited:    #2E7D52   /* 已去过（白字 5.3:1 ✅AA）*/
--visited-soft:#E2F1E7
--danger:     #C8493B   /* 危险操作（白字 4.8:1 ✅AA）*/
--danger-soft:#FBEAE7
```

**类别色板**（图钉/图标底色，白字或 emoji 叠加，均 ≥ 3:1 图形对比）：

| 类别 | 色值 | 类别 | 色值 |
|------|------|------|------|
| 🌳 公园 | `#3D9970` | 📚 图书馆 | `#2FA8A0` |
| 🎠 游乐场 | `#F2822E` | 🛍️ 商场儿童区 | `#E06A8B` |
| 🐼 动物园/水族馆 | `#A06BC8` | 🏖️ 亲水玩沙 | `#2FA3C7` |
| 🏛️ 博物馆/科技馆 | `#6478D8` | 📍 其他 | `#8A8378` |

### 2.2 字体系统

```
--font-display: "ZCOOL KuaiLe", "PingFang SC", sans-serif;  /* 标题：俏皮手写感 */
--font-body:    -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;

/* 模块化比例 1.25，clamp() 流体缩放 */
--fs-xs:   12px   辅助标签/徽章
--fs-sm:   13px   次要信息/地址
--fs-base: 15px   正文/列表
--fs-md:   17px   卡片标题/按钮
--fs-lg:   20px   抽屉标题
--fs-xl:   clamp(22px, 6vw, 28px)  页面大标题（display 字体）
```

### 2.3 间距 / 圆角 / 阴影 / 动效

```
/* 4pt 基准网格 */
--s1:4px  --s2:8px  --s3:12px  --s4:16px  --s6:24px  --s8:32px

/* 有机圆角：大面圆润、小件全胶囊 */
--r-sm:10px  --r-md:16px  --r-lg:22px  --r-pill:999px

/* 染色投影（绿色调，拒绝灰黑投影） */
--shadow-1: 0 2px 8px rgba(47,107,79,.10)
--shadow-2: 0 6px 20px rgba(47,107,79,.14)
--shadow-fab: 0 8px 22px rgba(47,107,79,.32)

/* 动效：指数缓动，只动 transform/opacity */
--ease-out: cubic-bezier(.22,1,.36,1)
--dur-fast: 160ms   --dur-normal: 260ms
@media (prefers-reduced-motion: reduce) { 全部动效降级为渐隐 }
```

---

## 三、信息架构与页面结构

```
┌─ 地图页（首页）────────────────────────┐
│  🔍 浮动搜索胶囊（顶部安全区内）          │
│  [全部][🌳公园][🎠游乐场]…  横滑分类条   │
│                                        │
│        （全屏地图 + 彩色图钉）            │
│                     ┌──┐               │
│                     │＋│ ← FAB 推荐地点 │
│                     └──┘               │
│  [🗺 地图]        [🎒 清单]   底部Tab   │
└────────────────────────────────────────┘
         │ 点击图钉              │ 点击＋
         ▼                      ▼
┌─ 地点详情（底部抽屉）─┐  ┌─ 推荐地点（底部抽屉）─┐
│ 名称·类别·评分        │  │ 名称 / 类别 / 位置     │
│ 实用信息 chips        │  │ 一句话推荐理由         │
│ 游玩记录·照片         │  │ 「帮助其他家庭 ✨」    │
└───────────────────────┘  └───────────────────────┘
```

**关键决策**：
1. **分类筛选条前置到地图页**（参考站核心模式）——家长找"游乐场"不用进清单，地图上直接筛
2. **FAB 语义升级**：「＋」= 推荐好去处，配合社区化文案，为后续社区共建留口
3. **双 Tab 不变**：地图（探索）/ 清单（管理），心智模型简单
4. **一切弹层皆底部抽屉**：单手操作、手势下滑关闭，符合移动端习惯

---

## 四、核心组件规范

### 4.1 地图图钉（双编码：颜色=类别，徽章=状态）
- 44×52px，圆形主体 + 底部尖角，白色 2.5px 描边保证任何底图上可辨识
- 待探索：纯类别色 + emoji；已去过：右下角白色小圆 + 类别色 ✓
- 选中态：图钉放大 1.15× + 底部浮现名称气泡

### 4.2 搜索胶囊
- 全圆角（999px）、微暖白底、`--shadow-2`、左侧 🔍 图标
- 输入中出现 ✕ 清除钮（视觉 32px / 热区 44px）
- 结果列表：圆角 16px 浮层，标题 15px + 地址 13px 单行截断

### 4.3 分类筛选 Chips
- 未选中：`--surface` 底 + `--line` 1.5px 描边 + `--ink-2` 文字
- 选中：类别浅底 + 类别色描边 + `--ink` 加粗文字（浅底不用白字，保证 AA）
- 横滑无滚动条，两端渐隐遮罩提示可滑

### 4.4 地点卡片（清单页）
- 22px 圆角 + `--shadow-1`，hover/按压上浮 2px
- 左：46px 圆角图标块（类别浅底+emoji）；中：名称 + 评分/次数/距离；右：状态徽章
- 待探索徽章=`--sun-soft` 底；已去过徽章=`--visited-soft` 底

### 4.5 底部抽屉
- 顶部圆角 22px + 42×5px grabber，max-height 86vh
- 打开：translateY 105%→0，260ms ease-out；遮罩 45% 深云杉
- 下滑手势关闭；同时只允许一个抽屉

### 4.6 按钮体系
| 类型 | 规格 |
|------|------|
| 主按钮 | `--spruce` 底白字，r-md，高 50px，17px 加粗，按压 scale(.98) |
| 次按钮 | `--surface` 底 + `--line` 描边 + `--ink-2` |
| 危险按钮 | `--danger-soft` 底 + `--danger` 文字 |
| FAB | 58px 圆形 `--spruce` + `--shadow-fab`，选点模式变珊瑚红 ✕ |

### 4.7 空状态（教育式，非占位式）
- 大 emoji（44px）+ 一句行动指引 + 主按钮
- 例：「🎈 还没有标记任何地点 — 点右下角 ＋，推荐你们的第一个秘密基地吧」

---

## 五、无障碍与性能基线

- **对比度**：正文 ≥ 4.5:1，大字号/图标 ≥ 3:1（全部令牌已标注实测值）
- **触控目标**：可点元素 ≥ 44×44px（小视觉元素用伪元素扩热区）
- **键盘/读屏**：`:focus-visible` 2px `--spruce` 描边；图钉带 `aria-label="类别+名称+状态"`
- **动效**：`prefers-reduced-motion` 下全部降级为渐隐
- **性能**：图钉用内联 SVG dataURI（零请求）；动效只动 transform/opacity；图片最长边压至 1000px

---

## 六、落地建议（现有 index.html 迁移）

1. **第一步（纯样式）**：替换 `:root` 令牌 + 字体栈，页面气质即刻焕新，零逻辑改动
2. **第二步（结构）**：分类筛选条从清单页复制一份到地图页顶部（复用 `.chip` 组件与 `state.filter`）
3. **第三步（社区化）**：`drawerAdd` 文案改为"推荐好去处"，增加"一句话推荐理由"字段
4. **验证清单**：真机单手拇指热区走查 → 对比度抽测 → 弱网图钉渲染检查

> 原配套可交互原型已随历史设计交付清场删除；现役体验以 `index.html` 为准。
